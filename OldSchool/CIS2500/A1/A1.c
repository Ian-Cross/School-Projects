#include "A1.h"

int main (int argc, char **argv)
{
    FILE * templateFile;
    FILE * dataFile;
    FILE * outputFile;
    char input[255];

    if (argc != 4){
        printf("Not enough arguments (TemplateFile, DataFile, OutputFile)\n);
        exit(1);
    }

    
    return 0;
}
